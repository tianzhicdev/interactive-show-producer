{
  "name": "interactive-show-producer",
  "private": true,
  "scripts": {
    "dev": "pnpm --dir workers run dev & pnpm --dir web run dev",
    "dev:web": "pnpm --dir web run dev",
    "dev:api": "pnpm --dir workers run dev",
    "web:build": "pnpm --dir web run build",
    "db:migrate": "node scripts/apply-db-migrations.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "fflate": "0.8.2",
    "zod": "3.24.0"
  }
}
